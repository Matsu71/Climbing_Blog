# Climbing Blog — 作業方針

最終目標は世界一のクライミングブログ。達成済みという宣伝には使わない。

## 優先順位（2026-09-14 ユーザー指示）

1. 記事の量産ではなく、出典付きデータの収集・検証を最優先する。
2. 選手の身長・体重・リーチ、高難度課題の登攀者・日付・スタイル・グレード異論、怪我の治療・復帰の研究を拡充する。
3. 既存のChatGPT製構成を良いと仮定しない。既存サイトを広く調査して参考先を選び、分類・導線・文字量・配色を整える。
4. 記事着手の前に必須データ・出典・矛盾・限界の確認を終える。未収集の多い現段階では新規記事を優先しない。

## データ

- `data/evidence.mjs` が2026-09-14開始の採録データ。`data/catalog.mjs` 等の既存資料を自動的に再監査済みとしない。
- 数値ごとに source_id、元表記、単位、測定時点・掲載文脈を保存する。確認日を測定日にしない。
- 未確認はnull。写真からの体格推定、身長からの翼幅推定、足りない日付の補完、再登不明を0件にする処理は禁止。
- 登攀日・公表日を分ける。初登・再登・フラッシュ、スタート位置・別バージョンも分ける。
- グレードの提案・個人評価・合意を分ける。現在の世界ランキングは全対象の監査前に作らない。
- 医療は患者数と患部数、対照、追跡期間、アウトカム、限界、対象疾患を保持する。症例報告を治療の優劣の証拠にしない。
- 治療・減量・復帰許可を個人向けに処方しない。専門家レビュー未実施を明示する。
- 著作権のある記事・写真・動画を丸ごと転載しない。短い事実・独自の要約と出典を保持する。

## レイアウト・文章

参考先と採否は `project-docs/data-first-2026-09-14.md` に記録する。
短い具体的な見出しを使い、「未来を切り開く」等の抽象的なコピーを置かない。本文の情報量とUI文字量は別に考える。複数サイトの要素は統一したデザインにまとめる。

## 検証・公開

`npm run check` を実行。通常環境・CIではChromium/WebKitの回帰検査も行う。環境の制約による検査不能と、製品のテスト不合格を区別する。失敗した検査を通過済みとしない。
root / docs の互換ランタイムは同じソース・データを使用する。`tests/evidence.test.mjs` が差異を検出する。`dist/` は静的出力で、元データとは別に編集しない。
コミット・CI成功・公開成功は別状態。既存の公開設定を勝手に変更しない。残作業はqueueと状態資料へ具体的に記録し、世界一の達成とは扱わない。
